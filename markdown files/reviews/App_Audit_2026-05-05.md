# App Audit — Products & Customers Actions

**Date reviewed:** 2026-05-05
**Scope:** `app/(dashboard)/products/actions.ts`, `app/(dashboard)/customers/actions.ts`, `zod-schema/product.ts`
**Verdict:** REQUEST CHANGES

---

## What's Good

| Area | Detail |
|------|--------|
| SQL Injection | Drizzle ORM parameterized queries everywhere — no raw SQL |
| XSS | All output via React JSX (auto-escaped), no `dangerouslySetInnerHTML` |
| Input Validation | Zod `safeParse` on every mutation before any DB write |
| Auth on actions | Both action files check `getUser()` before mutating |
| Update schema | `updateProductSchema` and `updateCustomerSchema` both correctly omit `createdAt`/`updatedAt` |
| Cache invalidation | `revalidatePath` called after every mutation |

---

## Findings

### [MEDIUM] Design template field silently never saves to DB

**File:** `app/(dashboard)/products/actions.ts:25`

The `parseFormData` function has a double typo. The object key is `desingTemplates` (misspelled + plural) but the schema expects `designTemplate`. Zod will ignore the unknown key and the field will always be `undefined` in the DB — no error, no warning, data just disappears.

**Current code:**
```ts
desingTemplates: formData.get("desingTemplate") || undefined,
```

**Fix:**
```ts
designTemplate: formData.get("designTemplate") || undefined,
```

Also check the form `<input name="...">` attribute — it should be `designTemplate`.

---

### [MEDIUM] `createProduct` crashes on DB constraint errors

**File:** `app/(dashboard)/products/actions.ts:47`

No try/catch around `db.insert()`. Any database constraint violation (e.g. a unique product name) throws an unhandled 500. The `createCustomer` action already handles this correctly.

**Current code:**
```ts
await db.insert(products).values(parsed.data);
```

**Fix:**
```ts
try {
  await db.insert(products).values(parsed.data);
} catch (e) {
  if (
    e instanceof Error &&
    "code" in e &&
    (e as { code: string }).code === "23505"
  ) {
    return { errors: { name: ["A product with this name already exists"] } };
  }
  throw e;
}
```

---

### [LOW] Typo in auth error message

**File:** `app/(dashboard)/customers/actions.ts:38`

```ts
throw new Error("Unathorized");  // ← missing 'u'
```

**Fix:**
```ts
throw new Error("Unauthorized");
```

---

### [NOTE] Auth scope — no role check

Both action files verify the user is logged in but do not check for a specific role. Any authenticated Kinde user can create/update products and customers. Fine for a single-owner dashboard, but worth adding a role check if more user types are added later.

---

## Summary

| # | Severity | Issue | File | Status |
|---|----------|-------|------|--------|
| 1 | MEDIUM | Design template field never saves (typo) | `products/actions.ts:25` | Open |
| 2 | MEDIUM | No try/catch on `createProduct` DB insert | `products/actions.ts:47` | Open |
| 3 | LOW | Typo in error message: "Unathorized" | `customers/actions.ts:38` | Open |

Fix #1 first — silent data loss bugs are the hardest to notice. #2 is a UX-breaking crash. #3 is trivial.

See `debugging/Bug_Fixes.md` (Fix 3, 4, 5) for the exact code changes.
